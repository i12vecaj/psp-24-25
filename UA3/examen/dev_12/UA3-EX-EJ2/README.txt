Revisado por JD: 18/02/2025

Para este ejercicio he elegido UDP puesto que el propio problema hace incapie en que tiene que ser rapido y no pasa nada si hay perdida de 
paquetes, por lo que el modo mas eficiente es UDP

La solucion tiene un servidor que envia mensajes y lo sclientes que tu quieras crear, los clientes se crean indicando el puerto y una vez indicando
esperan los mensajes del servidor
